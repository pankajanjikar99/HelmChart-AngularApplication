apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-config"
data:
  config.json: |
    {
      "apiUrl": "{{ .Values.frontend.apiUrl }}",
      "env": "{{ .Values.namespace }}"
    }